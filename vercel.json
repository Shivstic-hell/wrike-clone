{
  "buildCommand": "cd packages/shared && npm run build && cd ../backend && npm run build",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
